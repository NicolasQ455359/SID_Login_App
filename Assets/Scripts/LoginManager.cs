using UnityEngine;

public class LoginManager : MonoBehaviour
{
    public void OnLoginButton()
    {
        Debug.Log("LoginManager se mantiene solo por compatibilidad con la escena. AuthManager procesa el login real.");
    }
}
